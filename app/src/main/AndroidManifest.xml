<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tahadroid.yallaorder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_fawry"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.base.BaseActivity"></activity>
        <activity android:name=".ui.restaurant.RestautrantMenuActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".ui.cart.AddCartActivity" />
        <activity android:name=".ui.restaurant.RestaurentsActivity" />
        <activity android:name=".ui.payment.PaymentActivity" />
        <activity android:name=".ui.signup.SignupActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.place.LocationActivity" />
        <activity android:name=".ui.password.ForgetPasswordActivity" />
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>

</manifest>